kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: frontend-allow-external
spec:
  podSelector:
    matchLabels:
      app: {{ .Release.Name }}-frontend
  ingress:
  - ports:
    - port: 80